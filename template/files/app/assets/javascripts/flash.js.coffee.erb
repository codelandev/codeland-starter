<%= @app_const_base %>.Flash =
  init: ->
    setTimeout( ->
      $('.flash').slideDown()
    , 100)
    if $('.flash').length > 0
      setTimeout( ->
        $('.flash').slideUp()
      , 5000)
    $(document).one 'click', ->
      $('.flash').slideUp()
